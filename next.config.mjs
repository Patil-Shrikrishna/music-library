/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // protocol: "https",
    domains: ["images.unsplash.com"],
    // port: "",
    // pathname: "/my-bucket/**",
  },
};

export default nextConfig;
